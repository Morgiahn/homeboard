server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    # Permet de communiquer avec l’API si CORS pas configuré
    # location /api {
    #     proxy_pass http://192.168.1.61:PORT;
    # }
}